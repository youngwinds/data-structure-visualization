import { IConfig } from '@dsv-charts/typings/config';

export const defaultConfig: IConfig = {
  type: undefined,
  layout: {
    padding: {
      top: 0,
      left: 0,
      right: 0,
      bottom: 0,
    },
  },
};
